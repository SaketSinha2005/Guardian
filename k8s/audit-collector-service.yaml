apiVersion: v1
kind: Service
metadata:
  name: audit-api-service
  namespace: guardian-system
spec:
  selector:
    app: audit-collector
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
  type: NodePort